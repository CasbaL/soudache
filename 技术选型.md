# 技术选型

> 仙侠搜打撤游戏 — 技术栈选择与架构设计

---

## 一、技术选型概述

### 选型原则

| 原则 | 说明 |
|---|---|
| 移动端优先 | iOS/Android 双平台，性能优化 |
| 开发效率 | 快速原型，迭代成本低 |
| 社区生态 | 文档完善，社区活跃 |
| 长期维护 | 引擎稳定，不会突然停更 |
| 扩展性 | 后续可扩展局域网联机 |

---

## 二、游戏引擎选择

### 候选引擎对比

| 引擎 | 优势 | 劣势 | 适合场景 |
|---|---|---|---|
| **Unity** | 生态完善、移动端优化好、C# 开发 | 收费模式变化、包体较大 | 商业项目、团队开发 |
| **Godot** | 开源免费、轻量级、GDScript 易学 | 移动端优化稍弱、3D 能力有限 | 独立游戏、小团队 |
| **Cocos Creator** | 国产、2D 强、微信小游戏支持好 | 3D 弱、国际社区小 | 国内市场、小游戏 |
| **Unreal** | 画面顶级、蓝图系统 | 包体巨大、移动端优化难 | 3A 级项目 |

### 推荐选择：Godot 4.x

**选择理由：**

```json
{
  "decision": "使用 Godot 4.x 作为游戏引擎",
  "reason": "开源免费无授权费用，2D 性能优秀，GDScript 易学易用，移动端导出成熟，社区活跃且持续更新",
  "alternatives": ["Unity", "Cocos Creator"],
  "tradeoff": "Godot 的 3D 能力相对较弱，但本项目是 2D 游戏，不影响；生态不如 Unity 完善，但核心功能齐全"
}
```

**Godot 4.x 关键特性：**

| 特性 | 说明 | 对本项目价值 |
|---|---|---|
| Vulkan 渲染 | 高性能 2D/3D 渲染 | 流畅的战斗特效 |
| GDScript 2.0 | 类型安全、性能提升 | 开发效率高 |
| 导出模板 | iOS/Android 原生导出 | 一键打包移动端 |
| 场景系统 | 节点树架构 | 代码复用、模块化 |
| 信号系统 | 事件驱动 | 解耦游戏逻辑 |
| 物理引擎 | 2D 物理完善 | 碰撞检测、弹幕系统 |

---

## 三、编程语言选择

### 主语言：GDScript

**选择理由：**
- Godot 原生语言，性能优化最好
- 语法类似 Python，易学易用
- 动态类型，开发速度快
- 与 Godot 编辑器深度集成

**GDScript 代码示例：**

```gdscript
# 角色控制器示例
class_name Player
extends CharacterBody2D

# 导出变量，可在编辑器中调整
@export var speed: float = 200.0
@export var max_health: int = 500
@export var current_health: int = 500

# 信号
signal health_changed(new_health: int)
signal player_died()

# 移动处理
func _physics_process(delta: float) -> void:
    var input_vector = Input.get_vector("move_left", "move_right", "move_up", "move_down")
    velocity = input_vector * speed
    move_and_slide()

# 受伤处理
func take_damage(damage: int) -> void:
    current_health -= damage
    health_changed.emit(current_health)
    
    if current_health <= 0:
        player_died.emit()
        die()

# 死亡处理
func die() -> void:
    # 播放死亡动画
    $AnimationPlayer.play("die")
    # 禁用输入
    set_physics_process(false)
    # 通知游戏管理器
    GameManager.on_player_died()
```

### 辅助语言：C#（可选）

**使用场景：**
- 复杂的数据处理（地图生成算法）
- 性能关键的计算（路径寻找）
- 第三方库集成

**C# 代码示例：**

```csharp
// 地图生成算法示例
using Godot;
using System.Collections.Generic;

public partial class MapGenerator : Node
{
    private List<Room> GenerateCriticalPath(int layerCount, int roomCount)
    {
        var path = new List<Room>();
        var currentRoom = CreateStartRoom();
        path.Add(currentRoom);
        
        for (int i = 0; i < roomCount - 1; i++)
        {
            var nextRoom = CreateRandomRoom(currentRoom, layerCount);
            ConnectRooms(currentRoom, nextRoom);
            path.Add(nextRoom);
            currentRoom = nextRoom;
        }
        
        // 最后一个房间设为 Boss 房间
        var bossRoom = CreateBossRoom(layerCount);
        ConnectRooms(currentRoom, bossRoom);
        path.Add(bossRoom);
        
        return path;
    }
}
```

---

## 四、渲染方案

### 2D 渲染管线

```
┌─────────────────────────────────────────────────────────────┐
│                      渲染管线                                │
│                                                             │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐  │
│  │ 场景层   │ → │ 精灵层   │ → │ 特效层   │ → │ UI 层   │  │
│  │ Tilemap │    │ 角色/敌人│    │ 技能特效 │    │ HUD/UI │  │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘  │
│       ↓              ↓              ↓              ↓        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              后处理（可选）                           │   │
│  │  - 屏幕震动                                         │   │
│  │  - 受击闪红                                         │   │
│  │  - 全屏变暗                                         │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 精灵系统

**角色精灵：**
- 帧动画：8-12 帧/动画
- 方向：4 方向（上下左右）或 8 方向
- 状态：空闲、移动、攻击、受伤、死亡

**敌人精灵：**
- 帧动画：6-8 帧/动画
- 方向：4 方向
- 状态：空闲、移动、攻击、受伤、死亡

**精灵表格式：**
```
character_idle.png      # 空闲动画
character_move.png      # 移动动画
character_attack.png    # 攻击动画
character_hurt.png      # 受伤动画
character_die.png       # 死亡动画
```

### 特效系统

**粒子特效：**
- 技能释放：剑气、符咒、丹药
- 环境效果：火焰、烟雾、光点
- UI 特效：升级、获得物品

**Shader 特效：**
- 水墨渲染效果
- 受击闪红
- 护盾效果
- 冰冻效果

---

## 五、数据存储方案

### 本地存储架构

```
┌─────────────────────────────────────────────────────────────┐
│                     数据存储架构                             │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  存储层                              │   │
│  │                                                      │   │
│  │  ┌─────────┐    ┌─────────┐    ┌─────────┐         │   │
│  │  │ JSON    │    │ SQLite  │    │ 二进制  │         │   │
│  │  │ 配置文件│    │ 游戏数据│    │ 存档    │         │   │
│  │  └─────────┘    └─────────┘    └─────────┘         │   │
│  │                                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  数据类型                            │   │
│  │                                                      │   │
│  │  配置数据：游戏设置、键位绑定、语言                   │   │
│  │  存档数据：角色进度、建造状态、装备                   │   │
│  │  运行数据：当前局状态、临时变量                       │   │
│  │                                                      │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### JSON 配置文件

**用途：** 游戏设置、装备模板、敌人模板

**示例：装备模板**
```json
{
  "weapons": [
    {
      "id": "sword_001",
      "name": "青钢剑",
      "rarity": "white",
      "baseAttack": 40,
      "baseCritRate": 0.02,
      "icon": "res://assets/icons/sword_001.png"
    }
  ]
}
```

### SQLite 数据库

**用途：** 复杂数据查询、存档管理

**表结构：**
```sql
-- 装备表
CREATE TABLE equipment (
    id TEXT PRIMARY KEY,
    template_id TEXT NOT NULL,
    name TEXT NOT NULL,
    type TEXT NOT NULL,
    rarity TEXT NOT NULL,
    enhance_level INTEGER DEFAULT 0,
    owner_id TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 角色表
CREATE TABLE character (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    level INTEGER DEFAULT 1,
    realm TEXT DEFAULT 'liangqi',
    exp INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 建筑表
CREATE TABLE building (
    id TEXT PRIMARY KEY,
    type TEXT NOT NULL,
    level INTEGER DEFAULT 1,
    upgrade_started_at TIMESTAMP,
    upgrade_ends_at TIMESTAMP
);
```

### 二进制存档

**用途：** 快速读写、防篡改

**格式：**
```gdscript
# 保存存档
func save_game() -> void:
    var save_data = {
        "version": 1,
        "timestamp": Time.get_unix_time_from_system(),
        "player": {
            "name": player.name,
            "level": player.level,
            "realm": player.realm,
            "inventory": player.inventory.serialize()
        },
        "buildings": building_manager.serialize(),
        "equipment": equipment_manager.serialize()
    }
    
    var file = FileAccess.open("user://savegame.dat", FileAccess.WRITE)
    file.store_var(save_data)
    file.close()

# 加载存档
func load_game() -> bool:
    if not FileAccess.file_exists("user://savegame.dat"):
        return false
    
    var file = FileAccess.open("user://savegame.dat", FileAccess.READ)
    var save_data = file.get_var()
    file.close()
    
    # 验证版本
    if save_data.version != CURRENT_SAVE_VERSION:
        return migrate_save(save_data)
    
    # 恢复游戏状态
    restore_player(save_data.player)
    restore_buildings(save_data.buildings)
    restore_equipment(save_data.equipment)
    
    return true
```

---

## 六、网络方案（局域网联机）

### 网络架构

```
┌─────────────────────────────────────────────────────────────┐
│                    局域网联机架构                            │
│                                                             │
│  ┌─────────┐         ┌─────────┐         ┌─────────┐       │
│  │ 房主    │ ←─────→ │ 服务器  │ ←─────→ │ 客户端  │       │
│  │ (Host)  │  局域网  │ (P2P)   │  局域网  │(Client)│       │
│  └─────────┘         └─────────┘         └─────────┘       │
│                                                             │
│  同步内容：                                                 │
│  - 玩家位置和状态                                           │
│  - 敌人状态                                                 │
│  - 技能释放                                                 │
│  - 战斗结果                                                 │
│                                                             │
│  不同步：                                                   │
│  - 地图生成（各客户端本地生成，使用相同种子）                │
│  - 资源拾取（各客户端独立判定）                              │
│  - 建造进度（各客户端独立存储）                              │
└─────────────────────────────────────────────────────────────┘
```

### 网络技术选型

| 技术 | 说明 | 适用场景 |
|---|---|---|
| **ENet** | Godot 内置网络库 | 局域网 P2P 连接 |
| **RPC** | 远程过程调用 | 状态同步 |
| **MultiplayerSynchronizer** | Godot 4 节点同步 | 位置同步 |
| **MultiplayerSpawner** | Godot 4 对象同步 | 敌人/玩家同步 |

### 网络代码示例

```gdscript
# 网络管理器
class_name NetworkManager
extends Node

# 信号
signal player_connected(peer_id: int)
signal player_disconnected(peer_id: int)
signal connection_failed()

# 房主创建房间
func host_game(port: int = 7777) -> Error:
    var peer = ENetMultiplayerPeer.new()
    var error = peer.create_server(port, 4)  # 最多 4 人
    
    if error != OK:
        return error
    
    multiplayer.multiplayer_peer = peer
    multiplayer.peer_connected.connect(_on_peer_connected)
    multiplayer.peer_disconnected.connect(_on_peer_disconnected)
    
    return OK

# 客户端加入房间
func join_game(address: String, port: int = 7777) -> Error:
    var peer = ENetMultiplayerPeer.new()
    var error = peer.create_client(address, port)
    
    if error != OK:
        return error
    
    multiplayer.multiplayer_peer = peer
    multiplayer.connected_to_server.connect(_on_connected_to_server)
    multiplayer.connection_failed.connect(_on_connection_failed)
    
    return OK

# RPC 同步玩家位置
@rpc("any_peer", "unreliable")
func sync_player_position(position: Vector2, velocity: Vector2):
    var sender_id = multiplayer.get_remote_sender_id()
    # 更新其他玩家的位置
    update_remote_player(sender_id, position, velocity)

# RPC 同步技能释放
@rpc("any_peer", "reliable")
func sync_skill_cast(skill_id: String, target_position: Vector2):
    var sender_id = multiplayer.get_remote_sender_id()
    # 在所有客户端播放技能特效
    play_skill_effect(sender_id, skill_id, target_position)
```

---

## 七、构建工具

### 构建流程

```
┌─────────────────────────────────────────────────────────────┐
│                      构建流程                                │
│                                                             │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐  │
│  │ 开发    │ → │ 测试    │ → │ 打包    │ → │ 发布    │  │
│  │ Godot   │    │ 单元测试│    │ 导出    │    │ 应用商店│  │
│  │ 编辑器  │    │ 集成测试│    │ 模板    │    │         │  │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 导出配置

**Android 导出：**
```
平台：Android
架构：arm64-v8a, armeabi-v7a
最小 SDK：24 (Android 7.0)
目标 SDK：34 (Android 14)
签名：使用 Keystore 签名
```

**iOS 导出：**
```
平台：iOS
架构：arm64
最低版本：iOS 15.0
签名：使用 Xcode 签名
证书：Apple Developer 证书
```

### CI/CD 配置

**GitHub Actions 示例：**
```yaml
name: Build and Deploy

on:
  push:
    tags:
      - 'v*'

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Godot
        uses: chickensoft-games/setup-godot@v1
        with:
          version: 4.2.1
          export-template: android
      
      - name: Build Android
        run: |
          godot --headless --export-release "Android" build/game.apk
      
      - name: Upload to Play Store
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT }}
          packageName: com.xianxia.game
          releaseFiles: build/game.apk
          track: production

  build-ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Godot
        uses: chickensoft-games/setup-godot@v1
        with:
          version: 4.2.1
          export-template: ios
      
      - name: Build iOS
        run: |
          godot --headless --export-release "iOS" build/game.ipa
      
      - name: Upload to App Store
        uses: apple-actions/upload-testflight-build@v1
        with:
          app-path: build/game.ipa
          apple-id: ${{ secrets.APPLE_ID }}
          team-id: ${{ secrets.TEAM_ID }}
```

---

## 八、第三方库

### 推荐库

| 库 | 用途 | 链接 |
|---|---|---|
| **GodotSteam** | Steam 集成（可选） | https://github.com/Gramps/GodotSteam |
| **GodotFirebase** | Firebase 集成（可选） | https://github.com/GodotNuts/GodotFirebase |
| **Dialogue Manager** | 对话系统 | https://github.com/nathanhoad/godot_dialogue_manager |
| **GodotNotificationCenter** | 通知系统 | https://github.com/Iakobs/godot-notification-center |
| **GodotGooglePlayBilling** | Google Play 支付 | https://github.com/godotengine/godot-google-play-billing |

### 库集成示例

**Dialogue Manager 使用：**
```gdscript
# NPC 对话示例
func start_dialogue() -> void:
    var dialogue = DialogueManager.show_dialogue("res://dialogues/npc_001.dialogue")
    dialogue.dialogue_started.connect(_on_dialogue_started)
    dialogue.dialogue_ended.connect(_on_dialogue_ended)

func _on_dialogue_started() -> void:
    # 禁用玩家输入
    player.set_physics_process(false)

func _on_dialogue_ended() -> void:
    # 恢复玩家输入
    player.set_physics_process(true)
```

---

## 九、项目结构

### 目录结构

```
game/
├── .git/                    # Git 版本控制
├── .github/                 # GitHub Actions
│   └── workflows/
│       └── build.yml
├── addons/                  # 第三方插件
│   ├── dialogue_manager/
│   └── godotsteam/
├── assets/                  # 游戏资源
│   ├── audio/              # 音频文件
│   │   ├── bgm/           # 背景音乐
│   │   └── sfx/           # 音效
│   ├── fonts/              # 字体文件
│   ├── icons/              # 图标文件
│   ├── sprites/            # 精灵图
│   │   ├── characters/    # 角色精灵
│   │   ├── enemies/       # 敌人精灵
│   │   ├── effects/       # 特效精灵
│   │   └── tiles/         # 地图瓦片
│   └── ui/                 # UI 资源
├── data/                    # 游戏数据
│   ├── enemies.json        # 敌人模板
│   ├── equipment.json      # 装备模板
│   ├── skills.json         # 技能模板
│   └── buildings.json      # 建筑模板
├── docs/                    # 设计文档
│   ├── Boss设计文档.md
│   ├── 地图设计文档.md
│   ├── 建造系统设计.md
│   ├── 战斗系统设计.md
│   ├── 装备系统设计.md
│   └── 技术选型.md
├── scenes/                  # Godot 场景
│   ├── characters/         # 角色场景
│   ├── enemies/            # 敌人场景
│   ├── levels/             # 关卡场景
│   ├── ui/                 # UI 场景
│   └── effects/            # 特效场景
├── scripts/                 # GDScript 脚本
│   ├── autoload/           # 自动加载单例
│   │   ├── game_manager.gd
│   │   ├── audio_manager.gd
│   │   ├── save_manager.gd
│   │   └── network_manager.gd
│   ├── characters/         # 角色脚本
│   │   ├── player.gd
│   │   └── player_state_machine.gd
│   ├── enemies/            # 敌人脚本
│   │   ├── enemy.gd
│   │   ├── enemy_ai.gd
│   │   └── boss.gd
│   ├── systems/            # 系统脚本
│   │   ├── combat_system.gd
│   │   ├── inventory_system.gd
│   │   ├── equipment_system.gd
│   │   ├── building_system.gd
│   │   └── map_generator.gd
│   └── ui/                 # UI 脚本
│       ├── hud.gd
│       ├── inventory_ui.gd
│       └── building_ui.gd
├── project.godot            # Godot 项目文件
└── export_presets.cfg       # 导出配置
```

---

## 十、性能优化策略

### 移动端优化

| 优化项 | 方案 | 目标 |
|---|---|---|
| 帧率 | 30fps（可选 60fps） | 流畅运行 |
| 内存 | 对象池、资源卸载 | < 500MB |
| 包体 | 资源压缩、按需加载 | < 200MB |
| 电量 | 降低渲染复杂度 | 续航友好 |

### 渲染优化

| 优化项 | 方案 |
|---|---|
| 精灵批处理 | 合并相同材质的精灵 |
| 纹理图集 | 将小图合并成大图 |
| 视锥剔除 | 只渲染可见区域 |
| 层级优化 | 减少绘制调用 |

### 内存优化

| 优化项 | 方案 |
|---|---|
| 对象池 | 复用频繁创建的对象（弹幕、特效） |
| 资源卸载 | 离开场景后卸载未使用资源 |
| 延迟加载 | 按需加载资源 |
| 压缩纹理 | 使用 ASTC/ETC2 压缩 |

---

## 十一、开发工具

### 推荐工具

| 工具 | 用途 | 说明 |
|---|---|---|
| **Godot 4.2** | 游戏引擎 | 主开发工具 |
| **VS Code** | 代码编辑器 | GDScript 插件 |
| **Aseprite** | 像素画工具 | 制作精灵图 |
| **Tiled** | 地图编辑器 | 制作瓦片地图 |
| **Audacity** | 音频编辑 | 处理音效 |
| **Git** | 版本控制 | 代码管理 |

### VS Code 配置

**推荐插件：**
- Godot Tools：GDScript 语法高亮
- Godot Files：场景文件支持
- GitLens：Git 增强
- Error Lens：错误提示

---

## 十二、技术风险与应对

| 风险 | 影响 | 应对方案 |
|---|---|---|
| 移动端性能不足 | 卡顿、发热 | 性能监控、动态降级 |
| 联机同步问题 | 延迟、不同步 | 状态预测、回滚机制 |
| 存档损坏 | 玩家数据丢失 | 自动备份、云存档 |
| 跨平台兼容性 | 特定机型问题 | 充分测试、兼容模式 |
| 第三方库更新 | API 变化 | 锁定版本、及时更新 |
