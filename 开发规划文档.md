# 仙侠搜打撤 — 开发规划文档

> 基于项目全面审查，制定可执行的开发计划
> 更新日期：2026-07-27
> 版本：v1.1

---

## 一、项目现状评估

### 1.1 代码规模

| 指标 | 数值 | 备注 |
|---|---|---|
| GDScript 代码量 | ~13,000 行 | 含脚本、UI、系统、敌人、测试 |
| 脚本文件数 | 70+ | 其中 autoload 18 个，系统脚本 30+ |
| 自动加载单例 | 18 个 | `project.godot` 中注册 |
| 场景文件 | 28 个 | UI 20 + 游戏场景 8 |
| 数据文件 | 4 个 JSON | enemies / skills / equipment / factions |
| 测试文件 | 1 个 | `test_comprehensive.gd`，~1480 行 |
| 设计文档 | 6 份 | 战斗/地图/装备/Boss/建造/技术选型 |
| 资源文件 | ~30 个 | sprite、tileset、icon（像素风） |

### 1.2 系统完成度

| 系统 | 完成度 | 核心文件 | 说明 |
|---|---|---|---|
| 门派系统 | ✅ 90% | `faction_system.gd` / `faction_data.gd` | 3 门派数据完整，大招基础版 |
| 技能系统 | ✅ 85% | `skill_system.gd` / `skills.json` | 冷却/DOT/护盾/投射物/AOE，缺组合 |
| 装备系统 | ✅ 85% | `equipment_system.gd` / `equipment_data.gd` | 穿戴/属性汇总/套装，缺随机词缀 |
| 建造系统 | ✅ 80% | `building_system.gd` / `building_data.gd` | 9 建筑 5 级，缺产出自动化 |
| 境界系统 | ✅ 80% | `realm_system.gd` | 5 境界突破，缺特效 |
| 炼丹系统 | ✅ 80% | `alchemy_system.gd` | 10 丹方/队列，缺使用效果 |
| 地图生成 | ⚠️ 60% | `map_generator.gd` / `room.gd` / `room_templates.gd` | 主路径+分支生成完成，房间过渡未实现 |
| 迷雾系统 | ⚠️ 60% | `fog_system.gd` | 状态管理完成，缺渲染 |
| 敌人系统 | ⚠️ 50% | `enemy_spawner.gd` / `melee_enemy.gd` / `ranged_enemy.gd` | 数据驱动生成，AI 过于简单 |
| Boss 系统 | ⚠️ 30% | `boss_enemy.gd` / `bamboo_king.gd` 等 | 数据定义完整，阶段转换未实现 |
| 弹幕系统 | ⚠️ 40% | `bullet_pool.gd` / `bullet.gd` / `bullet_patterns.gd` | 基础版，缺弹幕模式 |
| 存档系统 | ✅ 85% | `save_manager.gd` | 序列化/反序列化/备份完整 |
| 音效系统 | ❌ 10% | `audio_manager.gd` | 框架存在但无音频资源 |
| 触控操作 | ❌ 0% | — | 只有键盘映射，无虚拟摇杆 |
| 游戏流程 | ❌ 20% | `open_world_level.gd` / `test_level.gd` | 场景未串联，无完整循环 |
| UI 系统 | ⚠️ 50% | `scripts/ui/*.gd`（20 个面板） | 各面板独立，缺统一管理 |

---

## 二、问题清单

### 2.1 阻断性问题（不修复无法游玩）

| ID | 问题 | 位置 | 影响 | 关联任务 |
|---|---|---|---|---|
| B-01 | 无完整游戏循环 | 项目级 | 玩家无法从门派选择玩到撤离 | T-01, T-02 |
| B-02 | 房间过渡未实现 | `open_world_level.gd` | 玩家无法在房间间移动 | T-05 |
| B-03 | 无虚拟摇杆 | `player.gd` | 移动端无法操作 | T-03 |
| B-04 | 无触控技能按钮 | `skill_bar.gd` | 移动端无法释放技能 | T-04 |
| B-05 | 无场景过渡动画 | 项目级 | 场景切换生硬 | T-01 |

### 2.2 体验性问题（影响游戏体验）

| ID | 问题 | 位置 | 影响 | 关联任务 |
|---|---|---|---|---|
| E-01 | Boss 阶段转换未实现 | `boss_enemy.gd` | Boss 战单调，无节奏变化 | T-09 |
| E-02 | 敌人 AI 过于简单 | `melee_enemy.gd` / `ranged_enemy.gd` | 追击→攻击循环，无策略性 | T-10 |
| E-03 | 无弹幕预警系统 | `warning_indicator.gd` | 玩家无法预判攻击，只能硬吃 | T-11 |
| E-04 | 无战斗反馈 | `combat_feedback.gd` | 打击感弱，无屏幕震动/闪红 | T-12 |
| E-05 | 无音效/BGM | `audio_manager.gd` | 沉浸感缺失 | T-22, T-23 |
| E-06 | 伤害公式过于简单 | `player.gd:153` | `damage - defense`，后期防御堆高伤害趋近 0 | T-14 |
| E-07 | 无新手教程 | 项目级 | 新玩家不知道怎么玩 | T-28 |
| E-08 | 门派大招效果单一 | `faction_system.gd:113` | 只有 circle_aoe 和 self_heal 两种 | T-16 |
| E-09 | 装备无随机词缀 | `equipment_generator.gd` | 掉落物缺乏惊喜感 | 阶段三 |
| E-10 | 撤离奖励无差异化 | `extraction_point.gd` | 深层撤离和浅层撤离奖励相同 | T-06 |

### 2.3 架构性问题（影响开发效率）

| ID | 问题 | 位置 | 影响 | 关联任务 |
|---|---|---|---|---|
| A-01 | GameManager 职责过重 | `game_manager.gd` | 同时管状态/玩家/背包/仓库/装备引用 | D-03 |
| A-02 | autoload 单例过多（18个） | `project.godot` | 初始化顺序敏感，启动慢 | D-06 |
| A-03 | 资源数据结构不一致 | 多处 | 背包 `Array[Dictionary]`、仓库 `Dictionary`、装备 `EquipmentData` 混用 | D-02 |
| A-04 | 无对象池 | `bullet.gd` / `damage_number.gd` | 每次 `new()` 创建节点，GC 卡顿 | T-13 |
| A-05 | 硬编码数值 | `building_system.gd:66` / `realm_system.gd:10` | 平衡性调整需改代码 | D-05 |
| A-06 | 碰撞层配置缺失 | `enemy_spawner.gd:108` | 代码中硬设 collision_layer/mask，未用项目配置 | 阶段二 |
| A-07 | 测试覆盖不足 | `tests/test_comprehensive.gd` | 只有 1 个测试文件，未覆盖 Boss/地图边界 | 阶段四 |

---

## 三、分阶段开发计划

### 阶段一：可玩原型（第 1-3 周）

> 目标：串联完整的一局游戏流程，移动端可操作
> 前置依赖：无（所有任务从零开始）
> 阶段产出：可在移动端完成一局"选门派→探索→战斗→撤离"的完整流程

#### 任务依赖图

```
T-01 场景管理器
  └─→ T-02 流程串联 ──→ T-06 撤离机制
                        ├─→ T-07 死亡处理
                        └─→ T-08 洞府入口
T-03 虚拟摇杆 ──┐
T-04 技能触控 ──┤
                └─→ T-05 房间过渡（需要摇杆可用来测试）
```

#### 任务清单

| 任务 ID | 任务 | 优先级 | 工作量 | 前置依赖 | 验收标准 |
|---|---|---|---|---|---|
| T-01 | 创建场景管理器 | P0 | 1天 | 无 | `SceneTransition` 单例，支持场景切换+淡入淡出 |
| T-02 | 串联游戏流程 | P0 | 2天 | T-01 | faction_select → haven → open_world → 战斗 → 撤离/死亡 → 结算 |
| T-03 | 实现虚拟摇杆 | P0 | 2天 | 无 | 左半屏触控移动，8 方向，松手停止 |
| T-04 | 技能按钮触控 | P0 | 1天 | 无 | 底部 4 个按钮（3 技能+大招），触控触发 |
| T-05 | 房间过渡逻辑 | P0 | 2天 | T-02 | 到达房间边缘 → 加载相邻房间 → 显示过渡 |
| T-06 | 撤离机制 | P0 | 1天 | T-05 | 进入撤离点 → 确认 → 保存物资 → 返回洞府 |
| T-07 | 死亡处理 | P0 | 0.5天 | T-02 | 死亡 → 丢失背包 → 返回洞府 |
| T-08 | 洞府主界面功能入口 | P1 | 1天 | T-02 | haven_main 各按钮可点击进入对应面板 |

#### 文件级实施计划

**T-01 场景管理器（新建 1 文件）：**
- 新建：`scripts/autoload/scene_transition.gd`
- 修改：`project.godot`（添加 autoload 注册）
- 实现要点：
  - `change_scene(path)` — 带淡入淡出的场景切换
  - `fade_out()` / `fade_in()` — 使用 `ColorRect` + `Tween`
  - 信号：`scene_changed(scene_path)`
  - 保留 `CanvasLayer` 层级确保遮罩在最上层

```gdscript
# scripts/autoload/scene_transition.gd
extends Node

signal scene_changed(scene_path: String)

var _overlay: ColorRect
var _tween: Tween

func _ready() -> void:
    # 创建全屏黑色遮罩
    var canvas = CanvasLayer.new()
    canvas.layer = 100
    add_child(canvas)
    _overlay = ColorRect.new()
    _overlay.color = Color.BLACK
    _overlay.set_anchors_preset(Control.PRESET_FULL_RECT)
    _overlay.modulate.a = 0.0
    canvas.add_child(_overlay)

func change_scene(path: String) -> void:
    await _fade_out(0.3)
    get_tree().change_scene_to_file(path)
    await get_tree().process_frame  # 等一帧让新场景加载
    await _fade_in(0.3)
    scene_changed.emit(path)

func _fade_out(duration: float) -> void:
    _tween = create_tween()
    _tween.tween_property(_overlay, "modulate:a", 1.0, duration)
    await _tween.finished

func _fade_in(duration: float) -> void:
    _tween = create_tween()
    _tween.tween_property(_overlay, "modulate:a", 0.0, duration)
    await _tween.finished
```

**T-02 串联游戏流程（修改 3-4 文件）：**
- 修改：`scripts/ui/faction_select_screen.gd` — 选择门派后调用 `SceneTransition.change_scene()` 进入洞府
- 修改：`scripts/ui/haven_main.gd` — "出发"按钮触发进入 `open_world` 场景
- 修改：`scripts/levels/open_world_level.gd` — 初始化时生成地图、加载第一个房间、生成玩家
- 修改：`scripts/autoload/game_manager.gd` — `victory()` / `game_over()` 中调用场景切换回洞府

**T-03 虚拟摇杆（新建 1 文件，修改 1 文件）：**
- 新建：`scripts/ui/virtual_joystick.gd`（含场景 `scenes/ui/virtual_joystick.tscn`）
- 修改：`scripts/characters/player.gd` — `_physics_process` 中读取摇杆输出
- 实现要点：
  - 左半屏 `_input` 检测 `InputEventScreenTouch` / `InputEventScreenDrag`
  - 内圈跟随手指，外圈固定（或也跟随首次触控位置）
  - 输出 `Vector2` 方向，通过全局变量或信号传递给 Player
  - 松手时内圈回弹到中心

```gdscript
# scripts/ui/virtual_joystick.gd
extends Control

signal joystick_input(direction: Vector2)

@onready var outer_ring: TextureRect = $OuterRing
@onready var inner_knob: TextureRect = $InnerKnob

var _touch_index: int = -1
var _origin: Vector2 = Vector2.ZERO
var _max_distance: float = 80.0
var output: Vector2 = Vector2.ZERO  # Player 读取此值

func _input(event: InputEvent) -> void:
    if event is InputEventScreenTouch:
        if event.pressed and event.position.x < get_viewport_rect().size.x / 2:
            _touch_index = event.index
            _origin = event.position
            _update_visuals(event.position)
        elif not event.pressed and event.index == _touch_index:
            _touch_index = -1
            output = Vector2.ZERO
            _reset_visuals()
    elif event is InputEventScreenDrag and event.index == _touch_index:
        var delta = event.position - _origin
        var clamped = delta.limit_length(_max_distance)
        output = clamped / _max_distance
        _update_visuals(_origin + clamped)
        joystick_input.emit(output)

func _update_visuals(pos: Vector2) -> void:
    inner_knob.global_position = pos - inner_knob.size / 2

func _reset_visuals() -> void:
    inner_knob.position = outer_ring.position + outer_ring.size / 2 - inner_knob.size / 2
```

**T-04 技能按钮触控（修改 1-2 文件）：**
- 修改：`scripts/ui/skill_bar.gd` — 按钮 `pressed` 信号触发对应技能
- 修改：`scenes/ui/skill_bar.tscn` — 确保 4 个按钮有正确的 `touch` 输入处理
- 修改：`scripts/ui/hud.gd` — 连接技能按钮到 Player 的 `use_skill_1/2/3()` 和 `use_ultimate()`

**T-05 房间过渡逻辑（修改 1-2 文件，新建 1 文件）：**
- 修改：`scripts/levels/open_world_level.gd` — 核心房间管理逻辑
- 修改：`scripts/systems/room.gd` — 添加入口/出口 `Area2D` 触发器
- 新建：`scripts/systems/room_transition.gd` — 过渡动画控制器
- 实现要点：
  - 每个房间的 4 个方向有 `Area2D` 出口触发器
  - 玩家进入触发器 → 检查相邻房间是否存在 → 加载新房间
  - 新房间实例化到场景树，设置玩家位置到对面入口
  - 过渡时播放淡入淡出或滑动动画
  - 旧房间释放（`queue_free()`）

```gdscript
# open_world_level.gd 中添加
func _on_exit_triggered(direction: String) -> void:
    var current_room = _rooms[_current_room_id]
    var neighbor_id = current_room.get_connection(direction)
    if neighbor_id == "" or neighbor_id not in _map_data.rooms:
        return  # 无相邻房间

    var exit_dir = direction
    var entry_dir = _opposite_direction(direction)  # north→south, east→west

    # 加载新房间
    var new_room = _load_room(neighbor_id)
    _position_player_at_entry(new_room, entry_dir)

    # 释放旧房间
    current_room.queue_free()
    _current_room_id = neighbor_id

    # 更新迷雾
    FogSystem.enter_room(neighbor_id)
```

**T-06 撤离机制（修改 2 文件）：**
- 修改：`scripts/systems/extraction_point.gd` — 添加玩家进入检测 + 确认弹窗
- 修改：`scripts/autoload/game_manager.gd` — `victory()` 中处理撤离逻辑
- 实现要点：
  - 玩家进入撤离点 `Area2D` → 显示确认对话框"是否撤离？"
  - 确认 → 调用 `GameManager.victory()` → 保存物资 → 切换回洞府场景
  - 撤离奖励根据当前层数差异化（第 1 层基础、第 2 层中等、第 3 层最高）

**T-07 死亡处理（修改 1 文件）：**
- 修改：`scripts/autoload/game_manager.gd` — `game_over()` 中处理死亡逻辑
- 实现要点：
  - 播放死亡动画 + 慢动作
  - 延迟 2 秒后显示"陨落"结算界面
  - 丢失背包中非宝库物品
  - 点击"返回洞府"切换场景

**T-08 洞府主界面功能入口（修改 1 文件）：**
- 修改：`scripts/ui/haven_main.gd` — 连接各按钮到对应面板的显示/隐藏
- 修改：`scenes/ui/haven_main.tscn` — 确保按钮节点命名正确

---

### 阶段二：战斗体验（第 4-6 周）

> 目标：战斗有深度、有反馈、有挑战
> 前置依赖：阶段一完成（需要可操作的角色和房间系统）
> 阶段产出：Boss 战有节奏变化，敌人有策略性，战斗打击感达标

#### 任务依赖图

```
T-14 伤害公式（独立，可并行）
T-13 对象池（独立，可并行）

T-09 Boss 阶段转换
  └─→ T-16 门派大招完善（需要阶段框架）

T-10 敌人 AI 状态机
  └─→ T-11 弹幕预警（需要 AI 触发攻击）

T-12 战斗反馈（依赖 T-14 伤害公式）

T-15 技能组合（依赖 T-09 Boss 框架验证）
```

#### 任务清单

| 任务 ID | 任务 | 优先级 | 工作量 | 前置依赖 | 验收标准 |
|---|---|---|---|---|---|
| T-09 | Boss 阶段转换 | P0 | 2天 | 阶段一 | 血量 70%/40% 切换攻击模式，转场提示 |
| T-10 | 敌人 AI 状态机 | P1 | 3天 | 阶段一 | 巡逻/追击/攻击/逃跑/远程走位 5 种状态 |
| T-11 | 弹幕预警系统 | P1 | 2天 | T-10 | 红色扇形/圆形/直线预警，0.3-0.8s 反应时间 |
| T-12 | 战斗反馈增强 | P1 | 2天 | T-14 | 屏幕震动、受击闪红、暴击放大、慢动作击杀 |
| T-13 | 对象池实现 | P1 | 1天 | 无 | 子弹池、伤害数字池、特效池 |
| T-14 | 伤害公式重构 | P1 | 1天 | 无 | `基础攻击 × 技能倍率 × (1 - 防御/(防御+100)) × 暴击` |
| T-15 | 技能组合系统 | P2 | 2天 | T-09 | 实现 3 组组合效果 |
| T-16 | 门派大招完善 | P2 | 1天 | T-09 | 剑修万剑归宗、符修天雷阵、丹修九转还魂 |

#### 文件级实施计划

**T-09 Boss 阶段转换（修改 4 文件）：**
- 修改：`scripts/enemies/boss_enemy.gd` — 添加 `_check_phase_transition()` 和 `_enter_phase()`
- 修改：`scripts/enemies/bosses/bamboo_king.gd` — 竹妖王 3 阶段具体行为
- 修改：`scripts/enemies/bosses/fire_demon.gd` — 火魔 3 阶段具体行为
- 修改：`scripts/enemies/bosses/tianji_elder.gd` — 天机老人 3 阶段具体行为
- 实现要点：
  - `_process()` 中每帧检查血量百分比
  - 阶段切换时：暂停 AI 0.5s → 播放特效 → 切换攻击模式列表 → 恢复 AI
  - 攻击模式从 `enemies.json` 的 `phases[].attacks` 读取
  - 破绽窗口：阶段 3 特定攻击后降低防御 3-5 秒

```gdscript
# boss_enemy.gd 核心逻辑
var current_phase: int = 0
var phase_data: Array = []
var is_transitioning: bool = false

func _process(delta: float) -> void:
    if is_transitioning:
        return
    _check_phase_transition()
    # ... 正常 AI 逻辑

func _check_phase_transition() -> void:
    var hp_pct = float(current_health) / float(max_health) * 100.0
    for i in range(phase_data.size()):
        var phase = phase_data[i]
        var hp_range = phase.get("healthRange", [100, 0])
        if hp_pct <= hp_range[0] and hp_pct > hp_range[1]:
            if i != current_phase:
                _enter_phase(i)

func _enter_phase(phase_idx: int) -> void:
    is_transitioning = true
    current_phase = phase_idx
    var phase = phase_data[phase_idx]

    # 显示转场提示
    CombatFeedback.show_phase_announcement(enemy_name + " 进入 " + phase.get("name", "") + "！")

    # 切换攻击模式
    var attacks = phase.get("attacks", [])
    _update_attack_patterns(attacks)

    # 攻击速度倍率
    var speed_mult = phase.get("attackSpeed", 1.0)
    attack_cooldown = base_attack_cooldown / speed_mult

    await get_tree().create_timer(1.0).timeout
    is_transitioning = false
```

**T-10 敌人 AI 状态机（修改 4 文件）：**
- 修改：`scripts/enemies/enemy.gd` — 添加通用状态机框架
- 修改：`scripts/enemies/melee_enemy.gd` — 近战 AI：巡逻→追击→攻击
- 修改：`scripts/enemies/ranged_enemy.gd` — 远程 AI：追击→射程判断→射击→后退
- 修改：`scripts/enemies/elite_enemy.gd` — 精英 AI：追击→技能→防御→狂暴
- 新建：`scripts/enemies/enemy_state_machine.gd`（可选，提取为独立组件）

```
状态机枚举：
enum State { IDLE, PATROL, CHASE, ATTACK, RETREAT, STUNNED, DEAD }

转换规则：
IDLE     → 检测到玩家（detect_range）  → CHASE
PATROL   → 检测到玩家                  → CHASE
CHASE    → 进入攻击范围（attack_range） → ATTACK
CHASE    → 玩家离开感知范围（1.5x）     → IDLE
ATTACK   → 攻击冷却结束 + 距离远        → CHASE
ATTACK   → 攻击冷却结束 + 距离近        → ATTACK
RETREAT  → 后退到安全距离               → ATTACK
任意状态 → 被定身                       → STUNNED
STUNNED  → 定身结束                     → CHASE
```

**T-11 弹幕预警系统（修改 1 文件，新建 1 文件）：**
- 修改：`scripts/systems/warning_indicator.gd` — 完善预警绘制
- 新建：`scripts/systems/bullet_patterns.gd` 扩展 — 添加更多弹幕模式
- 实现要点：
  - 3 种预警形状：圆形（`draw_circle`）、扇形（`draw_arc` + `draw_line`）、直线（`draw_line`）
  - 预警颜色：红色（伤害）、紫色（持续区域）、黄色（控制）
  - 预警时间 0.3-0.8 秒，渐变透明度表示紧迫程度
  - 预警结束后触发实际攻击

**T-12 战斗反馈增强（修改 2 文件）：**
- 修改：`scripts/systems/combat_feedback.gd` — 添加屏幕震动、闪红、慢动作
- 修改：`scripts/ui/damage_number.gd` — 暴击放大、治疗绿色、护盾蓝色
- 实现要点：
  - 屏幕震动：`get_viewport().set_screen_transform()` + `Tween` 抖动
  - 受击闪红：`CanvasLayer` 红色 `ColorRect` 短暂显示
  - 暴击放大：伤害数字 `scale` 从 1.5 缓动到 1.0
  - 慢动作击杀：Boss 死亡时 `Engine.time_scale = 0.3` 持续 1 秒

**T-13 对象池实现（修改 3 文件）：**
- 修改：`scripts/systems/bullet_pool.gd` — 完善子弹池（当前已有基础版）
- 修改：`scripts/ui/damage_number.gd` — 添加对象池复用
- 新建：`scripts/systems/object_pool.gd` — 通用对象池类

```gdscript
# 通用对象池
class_name ObjectPool
extends RefCounted

var _pool: Array[Node] = []
var _scene: PackedScene
var _parent: Node

func _init(scene: PackedScene, parent: Node, initial_size: int = 10) -> void:
    _scene = scene
    _parent = parent
    for i in range(initial_size):
        var obj = _scene.instantiate()
        obj.visible = false
        obj.set_process(false)
        obj.set_physics_process(false)
        _parent.add_child(obj)
        _pool.append(obj)

func acquire() -> Node:
    var obj: Node
    if _pool.size() > 0:
        obj = _pool.pop_back()
    else:
        obj = _scene.instantiate()
        _parent.add_child(obj)
    obj.visible = true
    obj.set_process(true)
    obj.set_physics_process(true)
    return obj

func release(obj: Node) -> void:
    obj.visible = false
    obj.set_process(false)
    obj.set_physics_process(false)
    _pool.append(obj)
```

**T-14 伤害公式重构（修改 2 文件）：**
- 修改：`scripts/characters/player.gd:153` — `attack_enemy()` 中使用新公式
- 修改：`scripts/enemies/enemy.gd` — `take_damage()` 中使用新公式
- 新公式：`实际伤害 = 基础攻击力 × 技能倍率 × (1 - 防御/(防御 + 100)) × 暴击倍率`
- 保底伤害：最少 1 点
- 暴击倍率：默认 1.5，受装备/境界加成

```gdscript
# 统一伤害计算函数（可放在 GameManager 或独立 util 中）
static func calculate_damage(base_attack: int, skill_mult: float,
                              target_defense: int, crit_rate: float,
                              crit_damage: float) -> Dictionary:
    var is_crit = randf() < crit_rate
    var crit_mult = crit_damage if is_crit else 1.0
    var defense_reduction = 1.0 - (float(target_defense) / (float(target_defense) + 100.0))
    var raw = base_attack * skill_mult * defense_reduction * crit_mult
    var final_damage = maxi(1, roundi(raw))
    return {"damage": final_damage, "is_crit": is_crit}
```

---

### 阶段三：内容扩充（第 7-10 周）

> 目标：增加游戏内容深度和重玩价值
> 前置依赖：阶段二完成（需要战斗系统可用才能测试新内容）
> 阶段产出：每层 8+ 种房间模板，3 层各有特色机制，音效 BGM 完整

#### 任务依赖图

```
T-17 房间模板 ──→ T-18 环境交互（在模板中添加环境元素）
              └─→ T-20 隐藏房间（在模板中定义触发条件）

T-19 NPC 事件（独立）

T-21 敌人变种（独立，需要 enemies.json）

T-22 音效 ──→ T-23 BGM（共享 AudioManager 框架）

T-24 掉落物（独立）

T-25 灵田自动化（独立）
```

#### 任务清单

| 任务 ID | 任务 | 优先级 | 工作量 | 前置依赖 | 验收标准 |
|---|---|---|---|---|---|
| T-17 | 房间模板扩充 | P1 | 3天 | 阶段二 | 每层 8+ 种房间模板 |
| T-18 | 环境交互机制 | P1 | 2天 | T-17 | 层特有机制：竹林减速、火焰灼伤、机关陷阱 |
| T-19 | NPC 事件系统 | P1 | 2天 | 阶段二 | 商人交易、随机事件、被困修士救援 |
| T-20 | 隐藏房间实现 | P2 | 1天 | T-17 | 击败精英/触摸石碑后出现隐藏通道 |
| T-21 | 更多敌人变种 | P2 | 2天 | T-10 | 每层增加 2-3 种敌人变种 |
| T-22 | 音效系统 | P2 | 2天 | 无 | 攻击/受击/技能/死亡/Boss 战音效 |
| T-23 | BGM 系统 | P2 | 1天 | T-22 | 洞府/探索/Boss 战三套 BGM + 淡入淡出 |
| T-24 | 掉落物系统 | P2 | 1天 | 阶段二 | 敌人死亡后物品散落地面，玩家拾取 |
| T-25 | 灵田自动化 | P2 | 1天 | 阶段一 | 灵田定时产出灵草，离线也计算 |

#### 文件级实施计划

**T-17 房间模板扩充（修改 2 文件，新建 1 文件）：**
- 新建：`data/room_templates.json` — 将模板数据从代码中提取到 JSON
- 修改：`scripts/systems/room_templates.gd` — 从 JSON 加载模板
- 修改：`scripts/systems/map_generator.gd` — 使用新模板系统
- 每层目标模板数：
  - 第 1 层（幽竹林）：战斗 4 + 资源 3 + 事件 2 + 精英 2 + 隐藏 1 = 12 种
  - 第 2 层（火焰山）：战斗 4 + 资源 3 + 事件 2 + 精英 2 + 隐藏 1 = 12 种
  - 第 3 层（天机阁）：战斗 5 + 资源 3 + 事件 3 + 精英 2 + 隐藏 2 = 15 种

```json
// data/room_templates.json 结构
{
  "layer1": {
    "COMBAT": [
      {
        "id": "combat_bamboo_ambush",
        "name": "竹林伏击",
        "difficulty": 1,
        "enemies": [
          {"enemy_id": "bamboo_spirit", "count_min": 2, "count_max": 4}
        ],
        "resources": [
          {"resource_id": "herb", "amount_min": 1, "amount_max": 3}
        ],
        "hazards": [],
        "layout": "circular",
        "props": ["bamboo_cluster"]
      }
    ],
    "RESOURCE": [...],
    "EVENT": [...],
    "ELITE": [...],
    "SECRET": [...]
  }
}
```

**T-18 环境交互机制（修改 3 文件，新建 1 文件）：**
- 新建：`scripts/systems/hazard_system.gd` — 环境危害管理器
- 修改：`scripts/systems/room.gd` — 房间加载时实例化环境元素
- 修改：`scripts/characters/player.gd` — 进入危害区域时受到效果
- 修改：`data/room_templates.json` — 模板中定义 `hazards` 字段
- 环境机制：

| 层 | 环境元素 | 效果 | 视觉 |
|---|---|---|---|
| 幽竹林 | 竹丛 | 阻挡移动和视线 | 竹子 sprite |
| 幽竹林 | 溪流 | 移动速度 -50% | 水面动画 |
| 火焰山 | 熔岩 | 每秒 50 伤害 | 红色粒子 |
| 火焰山 | 喷火口 | 周期性喷射火焰 | 预警+火焰弹幕 |
| 天机阁 | 齿轮 | 旋转夹伤 | 旋转动画 |
| 天机阁 | 符文陷阱 | 定身 2 秒 | 地面符文闪光 |

**T-19 NPC 事件系统（修改 1 文件，新建 1 文件）：**
- 新建：`scripts/systems/event_system.gd` — 事件管理器
- 修改：`scripts/systems/npc_interaction_system.gd` — 扩展 NPC 对话
- 事件类型：
  - **商人**：买卖装备/丹药/材料
  - **随机事件**：选择 A/B 获得不同结果（风险/收益）
  - **被困修士**：救援后获得稀有物品或线索
  - **古碑**：阅读获得功法残页

**T-21 更多敌人变种（修改 2 文件）：**
- 修改：`data/enemies.json` — 添加新敌人数据
- 修改：`scripts/enemies/enemy.gd` — 支持更多行为模式
- 新增敌人：

| 层 | 新增敌人 | 特点 |
|---|---|---|
| 第 1 层 | 竹妖治疗者 | 远程治疗其他敌人 |
| 第 1 层 | 竹妖自爆者 | 接近后自爆，范围伤害 |
| 第 2 层 | 火灵群 | 3-5 只一组，速度快血量低 |
| 第 2 层 | 岩浆虫 | 死亡后留下熔岩区域 |
| 第 3 层 | 机关弓手 | 远程连射，有护盾 |
| 第 3 层 | 符文守卫 | 周期性释放控制技能 |

**T-22 音效系统（修改 1 文件）：**
- 修改：`scripts/autoload/audio_manager.gd` — 添加音效播放接口
- 音效列表（至少需要以下音效文件）：

| 事件 | 音效文件 | 触发位置 |
|---|---|---|
| 普攻命中 | `sfx_hit.ogg` | `player.gd:165` |
| 技能释放 | `sfx_skill.ogg` | `skill_system.gd:146` |
| 受击 | `sfx_hurt.ogg` | `player.gd:300` |
| 暴击 | `sfx_crit.ogg` | `player.gd:158` |
| 敌人死亡 | `sfx_enemy_die.ogg` | `enemy.gd` |
| 拾取物品 | `sfx_pickup.ogg` | 掉落物系统 |
| 撤离成功 | `sfx_extract.ogg` | `game_manager.gd:130` |
| Boss 登场 | `sfx_boss_intro.ogg` | `boss_enemy.gd` |

**T-24 掉落物系统（新建 2 文件）：**
- 新建：`scripts/systems/drop_item.gd` — 掉落物实体
- 新建：`scenes/resources/drop_item.tscn` — 掉落物场景
- 实现要点：
  - 敌人死亡 → 根据 `dropItems` 概率生成掉落物
  - 掉落物散开动画（随机方向抛出）
  - 玩家接近自动拾取（`Area2D` 检测）
  - 稀有度越高光效越强（白→绿→蓝→紫→金）

---

### 阶段四：打磨优化（第 11-13 周）

> 目标：优化性能、完善 UI、提升用户体验
> 前置依赖：阶段三完成（需要完整内容才能做平衡和测试）
> 阶段产出：30fps 稳定运行，新手可引导，数值平衡，iOS/Android 测试通过

#### 任务依赖图

```
T-14 伤害公式（已在阶段二完成）→ T-26 数值平衡调优

T-27 UI 统一优化（独立）

T-28 新手教程（需要完整游戏流程）

T-29 性能优化 ──→ T-31 移动端适配测试

T-30 存档安全（独立）

T-32 战斗力系统（独立）

T-33 成就系统（独立）
```

#### 任务清单

| 任务 ID | 任务 | 优先级 | 工作量 | 前置依赖 | 验收标准 |
|---|---|---|---|---|---|
| T-26 | 数值平衡调优 | P1 | 3天 | T-14 | 全层数值测试，境界/装备/强化曲线合理 |
| T-27 | UI 统一优化 | P1 | 3天 | 阶段三 | 统一风格，装备对比，背包整理 |
| T-28 | 新手教程 | P1 | 2天 | 阶段三 | 引导：移动→攻击→技能→撤离→建造 |
| T-29 | 性能优化 | P2 | 2天 | 阶段三 | 30fps 稳定，内存 <200MB，加载 <3s |
| T-30 | 存档安全 | P2 | 1天 | 无 | 存档校验和，防篡改 |
| T-31 | 移动端适配测试 | P1 | 2天 | T-29 | iOS/Android 真机测试，多分辨率适配 |
| T-32 | 战斗力系统 | P2 | 1天 | T-26 | 综合评分，用于推荐层数 |
| T-33 | 成就系统 | P3 | 2天 | 阶段三 | 首次通关/无伤 Boss/全装备等 |

#### 文件级实施计划

**T-26 数值平衡调优（修改 5+ 文件）：**
- 修改：`data/enemies.json` — 调整敌人属性数值
- 修改：`data/skills.json` — 调整技能伤害/冷却
- 修改：`scripts/systems/realm_system.gd` — 调整境界加成曲线
- 修改：`scripts/systems/enhance_system.gd` — 调整强化成功率
- 修改：`data/equipment.json` — 调整装备属性范围
- 平衡目标：

| 阶段 | 玩家攻击力 | 玩家防御力 | 玩家生命 | 敌人血量 | 单次受伤 | 容错次数 |
|---|---|---|---|---|---|---|
| 第 1 层小怪 | 100-150 | 30-50 | 500 | 200 | 30-60 | 8-15 |
| 第 1 层 Boss | 150-200 | 50-80 | 600 | 8000 | 80-150 | 4-7 |
| 第 2 层小怪 | 250-350 | 80-120 | 900 | 400 | 60-100 | 9-15 |
| 第 2 层 Boss | 350-450 | 120-160 | 1100 | 15000 | 150-250 | 4-7 |
| 第 3 层小怪 | 500-700 | 160-200 | 1500 | 600 | 100-150 | 10-15 |
| 第 3 层 Boss | 700-900 | 200-250 | 1800 | 25000 | 250-400 | 4-7 |

**T-27 UI 统一优化（修改 10+ UI 文件）：**
- 统一字体、颜色、按钮样式
- 添加装备对比（穿戴 vs 背包中）
- 背包排序（按类型/稀有度/最近获得）
- 战斗 HUD 优化（血条、技能 CD、大招充能）
- 小地图完善（显示已探索房间和当前位置）

**T-28 新手教程（新建 2 文件）：**
- 新建：`scripts/ui/tutorial_system.gd` — 教程管理器
- 新建：`scenes/ui/tutorial_overlay.tscn` — 教程遮罩层
- 实现要点：
  - 高亮目标区域 + 文字说明 + 箭头指引
  - 分步骤触发，每步完成后自动进入下一步
  - 教程进度保存，下次不再重复
  - 可在设置中手动重置教程

```
教程步骤：
1. "选择你的门派" → 高亮门派选择按钮
2. "这是你的洞府" → 介绍各建筑（高亮+文字）
3. "出发探索！" → 高亮出发按钮
4. "拖动摇杆移动" → 高亮摇杆区域
5. "角色会自动攻击" → 放置一个弱敌人
6. "点击技能按钮释放技能" → 高亮技能按钮
7. "到达撤离点可以保存物资" → 高亮撤离点
8. "回去建造更强的装备吧！" → 返回洞府
```

**T-29 性能优化（修改多文件）：**
- 同屏敌人上限：10 个（超出的排队等待）
- 特效上限：20 个
- 对象池覆盖率：子弹 100%、伤害数字 100%、特效 80%
- 加载优化：房间资源预加载下一房间
- 内存优化：释放已离开房间的资源
- 目标指标：

| 指标 | 目标值 | 测试方法 |
|---|---|---|
| 帧率 | ≥30fps（Boss 战） | Godot 内置性能监视器 |
| 内存 | <200MB | Android Profiler / Xcode Instruments |
| 首次加载 | <3s | 从点击图标到可操作 |
| 场景切换 | <1s | 淡入淡出时间 |
| 存档大小 | <1MB | 检查 `user://savegame.dat` |

---

## 四、技术债务清单

按修复紧急度排序，每项标注影响的源文件和修复方案：

| ID | 债务 | 影响范围 | 建议时机 | 工作量 | 修复方案 |
|---|---|---|---|---|---|
| D-01 | 创建 SceneTransition 单例 | 全局 | 阶段一 T-01 | 1天 | 新建 `scripts/autoload/scene_transition.gd`，注册到 `project.godot` |
| D-02 | 统一资源数据模型 | 背包/仓库/装备 | 阶段二 | 2天 | 新建 `class_name ItemData`，背包/仓库/装备统一使用 |
| D-03 | GameManager 拆分 | 全局 | 阶段三 | 2天 | 拆为 `PlayerData` + `InventorySystem` + `StorageSystem` |
| D-04 | 实现对象池 | 战斗系统 | 阶段二 T-13 | 1天 | 新建 `ObjectPool` 类，子弹/伤害数字/特效复用 |
| D-05 | 硬编码数值外部化 | 多系统 | 阶段三 | 2天 | 建筑升级/境界消耗/强化成功率等移入 JSON |
| D-06 | autoload 依赖梳理 | 全局 | 阶段四 | 1天 | 绘制依赖图，消除循环依赖，减少 autoload 数量 |

### D-02 资源数据模型统一方案

```gdscript
# 统一的物品基类
class_name ItemData
var id: String
var name: String
var type: String  # "resource", "equipment", "potion", "material"
var amount: int
var rarity: String  # "white", "green", "blue", "purple", "gold"
var metadata: Dictionary  # 类型特有数据

func to_dict() -> Dictionary:
    return {"id": id, "name": name, "type": type, "amount": amount, ...}

static func from_dict(data: Dictionary) -> ItemData:
    var item = ItemData.new()
    item.id = data.get("id", "")
    ...
    return item
```

### D-03 GameManager 拆分方案

```
GameManager（保留：游戏状态机 + 全局信号）
  ├── PlayerData（新：玩家属性、等级、经验）
  ├── InventorySystem（新：背包管理）
  └── StorageSystem（新：仓库管理）
```

---

## 五、里程碑时间线

```
第 1 周  ┃ T-01 场景管理器（1天） + T-02 流程串联（2天） + T-08 洞府入口（1天）
         ┃ → 可以从门派选择进入洞府，洞府各面板可访问
         ┃
第 2 周  ┃ T-03 虚拟摇杆（2天） + T-04 技能触控（1天） + T-05 房间过渡（2天）
         ┃ → 移动端可以操作角色在房间间移动
         ┃
第 3 周  ┃ T-06 撤离（1天） + T-07 死亡（0.5天） + 集成测试 + Bug 修复
         ┃ → ★ 里程碑1：完整的一局游戏可玩 ★
         ┃   验收：从门派选择到撤离/死亡的完整流程
         ┃
第 4 周  ┃ T-14 伤害公式（1天） + T-09 Boss 阶段（2天） + T-16 大招（1天）
         ┃ → Boss 战有 3 阶段变化，数值使用百分比减伤
         ┃
第 5 周  ┃ T-10 敌人 AI（3天） + T-11 弹幕预警（2天）
         ┃ → 敌人有多种行为，攻击可预判
         ┃
第 6 周  ┃ T-13 对象池（1天） + T-12 战斗反馈（2天） + T-15 组合（2天）
         ┃ → ★ 里程碑2：战斗体验达标 ★
         ┃   验收：Boss 战有节奏感，敌人有策略性，打击感强
         ┃
第 7 周  ┃ T-17 房间模板扩充（3天） + T-20 隐藏房间（1天）
         ┃ → 每层 8+ 种房间模板
         ┃
第 8 周  ┃ T-18 环境交互（2天） + T-19 NPC 事件（2天）
         ┃ → 3 层各有特色环境机制和 NPC 事件
         ┃
第 9 周  ┃ T-21 敌人变种（2天） + T-24 掉落物（1天） + T-25 灵田（1天）
         ┃ → 敌人种类丰富，掉落物可见可拾取
         ┃
第 10周  ┃ T-22 音效（2天） + T-23 BGM（1天） + 集成测试
         ┃ → ★ 里程碑3：内容扩充完成 ★
         ┃   验收：每层内容丰富，有音效和 BGM
         ┃
第 11周  ┃ T-26 数值平衡（3天） + T-27 UI 优化（2天）
         ┃ → 全层数值合理，UI 风格统一
         ┃
第 12周  ┃ T-28 新手教程（2天） + T-29 性能优化（2天） + T-31 适配测试（1天）
         ┃ → 新手可引导，30fps 稳定
         ┃
第 13周  ┃ T-30 存档安全（1天） + T-32 战斗力（1天） + T-33 成就（2天） + 最终测试
         ┃ → ★ 里程碑4：可发布版本 ★
         ┃   验收：iOS/Android 真机测试通过，数值平衡，无阻断性 Bug
```

---

## 六、风险与应对

| 风险 | 概率 | 影响 | 触发信号 | 应对措施 | 负责阶段 |
|---|---|---|---|---|---|
| 数值平衡反复调优耗时 | 高 | 阶段四延期 | 测试中发现秒杀/刮痧 | 阶段二就做初步平衡，阶段四微调；准备数值模拟脚本 | T-14, T-26 |
| 移动端性能不达标 | 中 | 需重构 | 帧率 <20fps | 阶段二就引入对象池，阶段四做性能测试；准备降级方案 | T-13, T-29 |
| 美术资源不足 | 高 | 视觉效果差 | 无美术人员 | 优先使用程序化生成（粒子/几何图形）；使用免费素材库 | 全阶段 |
| Boss 战设计复杂度超预期 | 中 | 阶段二延期 | 单个 Boss 超过 3 天 | 先实现竹妖王完整 3 阶段，火魔/天机复用模式 | T-09 |
| 存档兼容性问题 | 低 | 玩家数据丢失 | 存档加载报错 | 版本号迁移机制已实现，每阶段测试存档兼容性 | T-30 |
| 触控操作手感差 | 中 | 移动端体验差 | 玩家反馈操作不流畅 | 虚拟摇杆支持灵敏度调节，技能按钮支持自定义布局 | T-03, T-04 |
| 地图生成死循环 | 低 | 游戏卡死 | 房间数 < 预期 | 添加生成超时保护，失败时使用预设模板 | T-05 |

---

## 七、验收标准总览

### 里程碑 1：可玩原型（第 3 周末）

| 验收项 | 验收方法 | 通过条件 |
|---|---|---|
| 门派选择 → 洞府 | 点击门派进入洞府场景 | 无报错，场景正确加载 |
| 洞府 → 探索 | 点击出发进入 open_world | 地图已生成，玩家可操作 |
| 虚拟摇杆移动 | 触屏左半区拖动 | 角色 8 方向移动，松手停止 |
| 技能触控释放 | 点击底部技能按钮 | 技能释放，有冷却倒计时 |
| 房间过渡 | 走到房间边缘 | 加载新房间，玩家出现在对面入口 |
| 撤离成功 | 进入撤离点确认 | 物资保存，返回洞府 |
| 死亡处理 | 生命值归零 | 丢失背包，返回洞府 |
| 一局完整流程 | 从头玩到尾 | 无阻断性 Bug，流程顺畅 |

### 里程碑 2：战斗体验（第 6 周末）

| 验收项 | 验收方法 | 通过条件 |
|---|---|---|
| Boss 阶段转换 | 打 Boss 观察 | 70%/40% 血量切换阶段，有转场提示 |
| 敌人 AI 多样性 | 观察敌人行为 | 至少 3 种行为（追击/攻击/逃跑） |
| 弹幕预警 | 观察 Boss 攻击 | 红色预警 0.3-0.8s，可闪避 |
| 战斗反馈 | 攻击/受击 | 屏幕震动、受击闪红、暴击放大 |
| 伤害公式 | 检查伤害数字 | 使用百分比减伤，无 0 伤害 |
| 对象池 | 同屏 20+ 子弹 | 帧率 ≥25fps |
| 技能组合 | 使用组合技能 | 有额外效果提示 |
| 大招效果 | 充能满后释放 | 3 门派大招效果各异 |

### 里程碑 3：内容扩充（第 10 周末）

| 验收项 | 验收方法 | 通过条件 |
|---|---|---|
| 房间模板数量 | 检查 room_templates.json | 每层 ≥8 种 |
| 环境交互 | 进入各层房间 | 竹林减速/火焰灼伤/机关陷阱生效 |
| NPC 事件 | 遇到 NPC | 商人可交易，事件有选择 |
| 隐藏房间 | 击败精英后 | 隐藏通道出现 |
| 敌人变种 | 各层探索 | 每层 ≥4 种敌人 |
| 音效 | 攻击/受击/技能 | 有对应音效播放 |
| BGM | 切换场景 | 洞府/探索/Boss 各有 BGM |
| 掉落物 | 敌人死亡 | 物品散落地面，可拾取 |

### 里程碑 4：可发布版本（第 13 周末）

| 验收项 | 验收方法 | 通过条件 |
|---|---|---|
| 新手教程 | 首次进入游戏 | 8 步引导完整，可跳过 |
| 帧率 | Godot 性能监视器 | Boss 战 ≥30fps |
| 内存 | 真机测试 | <200MB |
| 加载速度 | 计时 | 首次加载 <3s，场景切换 <1s |
| iOS 适配 | iPhone 真机 | 触控正常，UI 不错位 |
| Android 适配 | Android 真机 | 触控正常，UI 不错位 |
| 存档安全 | 断电测试 | 存档不丢失，有备份 |
| 数值平衡 | 3 层通关测试 | 各层难度递增合理，无秒杀/刮痧 |
| 无阻断 Bug | 全流程测试 | 无崩溃、无卡死、无数据丢失 |

---

## 八、任务总览

### 按优先级统计

| 优先级 | 任务数 | 总工作量 | 说明 |
|---|---|---|---|
| P0（阻断性） | 8 个 | ~10 天 | 必须完成才能游玩 |
| P1（体验性） | 13 个 | ~25 天 | 核心体验保障 |
| P2（内容性） | 10 个 | ~15 天 | 内容丰富度 |
| P3（锦上添花） | 2 个 | ~3 天 | 成就等附加系统 |
| **合计** | **33 个** | **~53 天** | **13 周（含测试缓冲）** |

### 按阶段统计

| 阶段 | 周期 | 任务数 | 新建文件 | 修改文件 | 关键产出 |
|---|---|---|---|---|---|
| 阶段一 | 第 1-3 周 | 8 个 | 3 | 6 | 完整游戏循环 |
| 阶段二 | 第 4-6 周 | 8 个 | 2 | 10 | 战斗体验 |
| 阶段三 | 第 7-10 周 | 9 个 | 4 | 8 | 内容扩充 |
| 阶段四 | 第 11-13 周 | 8 个 | 2 | 15+ | 打磨优化 |

### 新建文件清单

| 文件路径 | 所属任务 | 说明 |
|---|---|---|
| `scripts/autoload/scene_transition.gd` | T-01 | 场景管理器 |
| `scripts/ui/virtual_joystick.gd` | T-03 | 虚拟摇杆 |
| `scenes/ui/virtual_joystick.tscn` | T-03 | 摇杆场景 |
| `scripts/systems/room_transition.gd` | T-05 | 房间过渡控制器 |
| `scripts/systems/object_pool.gd` | T-13 | 通用对象池 |
| `scripts/systems/hazard_system.gd` | T-18 | 环境危害管理器 |
| `scripts/systems/event_system.gd` | T-19 | NPC 事件管理器 |
| `scripts/systems/drop_item.gd` | T-24 | 掉落物实体 |
| `scenes/resources/drop_item.tscn` | T-24 | 掉落物场景 |
| `data/room_templates.json` | T-17 | 房间模板数据 |
| `scripts/ui/tutorial_system.gd` | T-28 | 教程管理器 |
| `scenes/ui/tutorial_overlay.tscn` | T-28 | 教程遮罩层 |
